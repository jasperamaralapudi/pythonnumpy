import numpy as np

arr = np.array([1, 2, 3, 4, 5])

# Element-wise operations
print("Array plus 2:", arr + 2)
print("Array squared:", arr ** 2)

# Aggregations
print("Sum:", np.sum(arr))
print("Mean:", np.mean(arr))
print("Standard Deviation:", np.std(arr))
